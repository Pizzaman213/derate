"""Load harness for the gateway.

Not part of the unit suite. ``python -m tests.load --help`` is the entry point;
``tests/test_load.py`` pins the SLOs this harness measures so a future change
cannot silently regress them.
"""
